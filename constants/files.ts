import { documentDirectory } from "expo-file-system";

export const IMAGES_DIR = `${documentDirectory}images/`;
export const RECORDINGS_DIR = `${documentDirectory}recordings/`;
export const VIDEOS_DIR = `${documentDirectory}videos/`;
export const THUMBNAILS_DIR = `${documentDirectory}thumbnails/`;
